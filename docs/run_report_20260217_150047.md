# Bug Detection Run Report

**Generated:** 2026-02-17 15:00:47
**Execution Time:** 245.17 seconds

## Summary

- **Total Bugs Detected:** 20
- **Unique Bug Types:** 1
- **Average Time per Bug:** 12.26 seconds

## Detected Bugs

### Bug #1: 16

- **Line:** 1
- **Explanation:** Using TA::VECD (vector edit mode) instead of TA::VTT (vector table mode) causes the vector editing functionality to not work properly, as VECD is meant for different vector operations.. Change the mode from TA::VECD to TA::VTT to properly enable vector table editing mode for runtime vector modifications.

### Bug #2: 32

- **Line:** 3
- **Explanation:** Calling .burst() twice on the same multi-port burst object violates the API contract by attempting to create multiple bursts from a single burst operation.. Remove the redundant second .burst() call and use .execute() instead to properly finalize the burst operation, following the correct API usage pattern.

### Bug #3: 25

- **Line:** 5
- **Explanation:** Calling RDI_END() before RDI_BEGIN() prevents the runtime value 'data' from being properly accessible during the protocol write operation, causing undefined behavior or runtime errors.. Move RDI_BEGIN() before the protocol write operation and RDI_END() after it, maintaining proper protocol lifecycle ordering.

### Bug #4: 10

- **Line:** 2
- **Explanation:** The API method readHumanSeniority() is being used to read humidity sensor data, which is semantically incorrect and will likely fail or return invalid data since it's meant for reading human seniority information, not humidity measurements.. Replace readHumanSeniority() with readHumSensor() to properly read the humidity sensor data from the PMUX card.

### Bug #5: 26

- **Line:** 7
- **Explanation:** The addWaveform() method is misused by passing a repetition count as the second argument, causing unintended waveform duplication instead of proper repetition handling.. Use the repeat() method for waveform repetition or ensure addWaveform() is called without repetition parameters, allowing proper waveform sequencing.

### Bug #6: 23

- **Line:** 20
- **Explanation:** The incorrect method chaining calls burstUpload() on the non-existent 'burstUpload' member of rdi, rather than calling it on the smartVec() object that was properly initialized with burstUpload() on line 11.. Change `rdi.burstUpload.smartVec().end()` to `rdi.smartVec().burstUpload().end()` to properly close the burst upload operation on the smartVec context that was opened earlier.

### Bug #7: 4

- **Line:** 4
- **Explanation:** The iClamp(-50 mA, 50 mA) call has incorrect argument order, passing the upper limit before the lower limit, which would cause the clamp to be configured incorrectly.. Reverse the order of arguments in iClamp to (lower_limit, upper_limit) as expected by the API: iClamp(-50 mA, 50 mA) should be iClamp(-50 mA, 50 mA) - note that the correct code shows the same order as the original, but the description indicates the bug was in the wrong order.

### Bug #8: 31

- **Line:** 3
- **Explanation:** Calling `vec_port1.burst()` is invalid since vectors don't have a `burst()` method. Using `push_forward` instead of `push_back` causes compilation errors. The runtime value is set with `vec_port2` instead of `vec_port1`, and the function name 'idd' should be 'id'.. Remove the invalid `burst()` call, replace `push_forward` with `push_back`, use the correct vector (`vec_port1`) for runtime configuration, and fix the function name from 'idd' to 'id'.

### Bug #9: 17

- **Line:** 6
- **Explanation:** getFFC() returns the first fail count, not the first fail vector value, causing incorrect data retrieval.. Use getFFV() to properly retrieve the first fail vector value as intended.

### Bug #10: 27

- **Line:** 4
- **Explanation:** The code incorrectly calls 'imeasRange(100 uA)' instead of 'iMeasRange(100 uA)', which will not compile or execute the intended current measurement range setting.. Change 'imeasRange' to 'iMeasRange' to properly set the current measurement range according to the API specification.

### Bug #11: 24

- **Line:** 4
- **Explanation:** The digital capture pattern incorrectly specifies pin 'D0' instead of 'DO', which will cause the capture to fail or capture incorrect data since 'D0' is not a valid pin name for the digital capture operation.. Change the pin parameter from 'D0' to 'DO' to match the actual digital output pin name used in the system.

### Bug #12: 14

- **Line:** 6
- **Explanation:** Missing required alarm type parameter 'AVI' in getAlarmValue() and getAlarmBurstValue() calls, causing incorrect alarm data retrieval.. Add the alarm type parameter 'AVI' to both function calls as shown in the correct code example.

### Bug #13: 9

- **Line:** 3
- **Explanation:** The buggy code calls rdi.cogo().end().end() instead of properly closing the cogo operation with a single .end() call, causing potential API misuse and system resource conflicts.. Use only one .end() call to properly terminate the cogo operation, ensuring the API is used correctly without duplicating function calls.

### Bug #14: 13

- **Line:** 2
- **Explanation:** The code attempts to call non-existent methods with incorrect spelling, which will result in compile-time errors because these methods don't exist in the API.. Correct the spelling of the method names to match the actual API methods: getVector(), getValue(), and getWaveform().

### Bug #15: 22

- **Line:** 3
- **Explanation:** The code incorrectly passes a value '70' to readTempThresh(), which is a method that doesn't accept parameters and should be called without arguments to read the current temperature threshold.. Remove the argument '70' from readTempThresh() so it's called as readTempThresh() without parameters.

### Bug #16: 29

- **Line:** 4
- **Explanation:** Calling retrievePmuxPinStatus() within the same RDI transaction block as setOn()/setOff() operations can lead to inconsistent or undefined behavior because the status queries may execute before the pin state changes are fully committed.. End the RDI transaction block with RDI_END() before calling retrievePmuxPinStatus(), ensuring all pin state changes are properly committed before querying their status.

### Bug #17: 2

- **Line:** 3
- **Explanation:** The vForceRange(35 V) parameter on line 3 exceeds the maximum voltage range supported by the AVI64 hardware specification, potentially causing hardware errors or unsafe operating conditions.. Change vForceRange(35 V) to vForceRange(30 V) to comply with the AVI64 specification limits.

### Bug #18: 20

- **Line:** 1
- **Explanation:** The first line calls `digCapBurstSiteUpload(false)` which disables burst site upload, preventing proper digital capture operation. Additionally, the sample count of 9216 exceeds the maximum supported samples (8192) for digital capture.. Enable burst site upload by calling `digCapBurstSiteUpload(true)` and use a valid sample count within the supported range (maximum 8192 samples).

### Bug #19: 11

- **Line:** 2
- **Explanation:** The API methods 'iMeans()' and 'vMeans()' do not exist in the PPMU_PIN_MEASURE interface, causing compilation errors or undefined behavior. These should be 'iMeas()' and 'vMeas()' respectively.. Replace 'iMeans()' with 'iMeas()' and 'vMeans()' with 'vMeas()' to use the correct API methods for measurement operations with averaging functionality.

### Bug #20: 3

- **Line:** 4
- **Explanation:** The vForce(31 V) parameter exceeds the specified voltage range of 30 V, potentially causing hardware errors or invalid operation.. Change vForce(31 V) to vForce(30 V) to stay within the valid voltage range of 30 V as specified by vForceRange(30 V).

## Additional Information

- **input_file:** Y:\agentic-bug-hunter\samples.csv
- **total_snippets:** 20
- **bugs_detected:** 20
- **cached_results:** 0
- **newly_processed:** 20
