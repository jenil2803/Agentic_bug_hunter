# Bug Detection Run Report

**Generated:** 2026-02-17 15:07:49
**Execution Time:** 238.05 seconds

## Summary

- **Total Bugs Detected:** 20
- **Unique Bug Types:** 1
- **Average Time per Bug:** 11.90 seconds

## Detected Bugs

### Bug #1: 16

- **Line:** 1
- **Explanation:** Using TA::VECD (vector edit mode) instead of TA::VTT (vector table mode) prevents proper vector manipulation during runtime editing.. Change vecEditMode parameter from TA::VECD to TA::VTT to enable correct vector table editing functionality.

### Bug #2: 32

- **Line:** 3
- **Explanation:** Calling .burst() twice on the same multi-port burst object violates the API contract by attempting to create nested bursts on an already-burst resource.. Remove the redundant second .burst() call and use .execute() instead to properly finalize the multi-port burst operation.

### Bug #3: 25

- **Line:** 5
- **Explanation:** Calling RDI_END() before RDI_BEGIN() prevents the runtime value 'data' from being properly recognized by the protocol write operation, causing undefined behavior or failed execution.. Move RDI_BEGIN() before RDI_END() to ensure proper protocol initialization and runtime value binding in the correct order.

### Bug #4: 10

- **Line:** 2
- **Explanation:** API functions are misused - using human seniority functions instead of humidity sensor functions, causing incorrect hardware operations and data retrieval.. Use the correct API methods readHumSensor() and getHumSensor() that correspond to the actual humidity sensor functionality being implemented.

### Bug #5: 26

- **Line:** 7
- **Explanation:** In the buggy code, `addWaveform(wav3, 2)` and `addWaveform(wav2, 3)` incorrectly pass repetition counts as second arguments, which changes the waveform behavior unexpectedly rather than simply adding waveforms multiple times.. Use separate calls to `addWaveform()` for each waveform instance, or use the appropriate method for specifying repetitions without mixing parameters incorrectly.

### Bug #6: 23

- **Line:** 20
- **Explanation:** The incorrect method chaining calls burstUpload() on the wrong object, attempting to call burstUpload() on a non-existent 'burstUpload' member of the rdi object rather than on the smartVec() object.. Change `rdi.burstUpload.smartVec().end()` to `rdi.smartVec().burstUpload().end()` to properly call burstUpload() on the smartVec() object that was started with the begin() call.

### Bug #7: 4

- **Line:** 4
- **Explanation:** The iClamp method call uses incorrect argument order: iClamp(50 mA, -50 mA) should be iClamp(-50 mA, 50 mA) to properly set the current clamp range.. Reverse the argument order in the iClamp method call to use the correct range specification: iClamp(-50 mA, 50 mA).

### Bug #8: 31

- **Line:** 3
- **Explanation:** The buggy code attempts to call `burst()` on a vector (which has no such method), uses incorrect `push_forward` method name instead of `push_back`, and passes an uninitialized/incorrectly populated vector to `runTimeVal`.. Remove the invalid `burst()` call, replace `push_forward` with `push_back`, ensure the vector is properly populated before passing to `runTimeVal`, and use the correct function name 'id' instead of 'idd'.

### Bug #9: 17

- **Line:** 6
- **Explanation:** getFFC() returns the first fail count, not the first fail vector value, causing incorrect data retrieval.. Use getFFV() to properly retrieve the first fail vector value as intended.

### Bug #10: 27

- **Line:** 4
- **Explanation:** The code incorrectly calls 'imeasRange(100 uA)' instead of 'iMeasRange(100 uA)', which will not compile or function as intended according to the RDI API documentation.. Change 'imeasRange' to 'iMeasRange' to properly specify the current measurement range as documented.

### Bug #11: 24

- **Line:** 4
- **Explanation:** The digital capture pattern incorrectly specifies pin "D0" instead of the required pin "DO", which will cause the capture to fail or capture incorrect data.. Change the pin parameter from "D0" to "DO" to match the expected digital output pin name for the SmartRDI.Digital capture functionality.

### Bug #12: 14

- **Line:** 6
- **Explanation:** Missing alarm identifier parameter in getAlarmValue() and getAlarmBurstValue() calls, which should specify the alarm type ("AVI") to retrieve the correct alarm data.. Add the alarm identifier "AVI" as a parameter to both getAlarmValue("AVI") and getAlarmBurstValue("AVI") calls to properly retrieve the specific alarm results.

### Bug #13: 9

- **Line:** 3
- **Explanation:** The buggy code calls rdi.cogo().end().end() instead of properly closing the cogo operation with a single .end() call, causing potential API misuse and system resource corruption.. Use only one .end() call to properly terminate the cogo operation, ensuring the API is used correctly according to its design.

### Bug #14: 13

- **Line:** 2
- **Explanation:** The code attempts to call non-existent methods with incorrect spelling, which will result in compile-time errors because these methods don't exist in the API.. Replace the misspelled method names with their correct counterparts: getVector(), getValue(), and getWaveform().

### Bug #15: 22

- **Line:** 3
- **Explanation:** The code incorrectly passes a value '70' to readTempThresh(), which is a method that doesn't accept parameters and should be called without arguments to read the current temperature threshold.. Remove the argument '70' from readTempThresh() so it's called as readTempThresh() without any parameters.

### Bug #16: 29

- **Line:** 4
- **Explanation:** The code attempts to retrieve PMUX pin statuses while the RDI transaction is still active, which may return stale or incorrect values due to pending operations not yet committed.. Move the retrievePmuxPinStatus() calls after RDI_END() to ensure all previous operations are properly executed and committed before querying the final pin states.

### Bug #17: 2

- **Line:** 2
- **Explanation:** The code attempts to set a voltage force range of 35V on the VS pin, but the hardware specification (AVI64) only supports up to 30V for this pin, causing a range violation error.. Change the vForceRange from 35V to 30V to comply with the AVI64 specification limits for the VS pin.

### Bug #18: 20

- **Line:** 1
- **Explanation:** The first line calls `digCapBurstSiteUpload(false)` which disables burst site upload, preventing proper digital capture operation. Additionally, the sample count of 9216 exceeds the maximum supported samples (8192) for digital capture.. Enable burst site upload by calling `digCapBurstSiteUpload(true)` and use a valid sample count (8192 or less) that fits within the supported capacity.

### Bug #19: 11

- **Line:** 2
- **Explanation:** The API functions 'iMeans()' and 'vMeans()' are not valid for specifying measurement operations, causing undefined behavior or compilation errors.. Replace 'iMeans()' with 'iMeas()' and 'vMeans()' with 'vMeas()' to properly specify measurement operations according to the PPMU_PIN_MEASURE API.

### Bug #20: 3

- **Line:** 4
- **Explanation:** The vForce(31 V) parameter exceeds the specified voltage range limit of 30 V, potentially causing hardware errors or invalid operation.. Change the voltage force value from 31 V to 30 V to stay within the specified voltage range limit.

## Additional Information

- **input_file:** Y:\agentic-bug-hunter\samples.csv
- **total_snippets:** 20
- **bugs_detected:** 20
- **cached_results:** 0
- **newly_processed:** 20
